#!/bin/sh
# postinst script for dssp-installer
#
# see: dh_installdeb(1)

set -e


case "$1" in
    configure)

	#echo -n "Downloading Dssp from the original website... "
	#curl "http://swift.cmbi.kun.nl/gv/dssp/HTML/dsspcmbi" > /usr/bin/dssp 2>/dev/null;
	#if [ $!=="1" ]; then
	#	echo "Done";
	#	chmod a+x /usr/bin/dssp;
	#else
	#        echo "Failed!";
        #	exit 1
	#fi

    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
